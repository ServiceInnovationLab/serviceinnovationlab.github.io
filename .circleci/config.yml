---
version: 2
jobs:
  test:
    docker:
      - image: circleci/ruby:2.6.3-node
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "Gemfile.lock" }}
      - run:
          name: Install dependencies
          command: >
            bundle check --path vendor/bundle || bundle install --deployment
      - run: bundle exec jekyll test
      - save_cache:
          key: v1-dependencies-{{ checksum "Gemfile.lock" }}
          paths:
            - vendor/bundle
  yamllint:
    docker:
      - image: circleci/python:3.7.2
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "Gemfile.lock" }}
      - run:
          name: Install yamllint
          command: sudo pip install --upgrade yamllint
      - run:
          name: Yaml linter
          command: yamllint .
  accessibility:
    docker:
      - image: circleci/ruby:2.6.3-node-browsers
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "Gemfile.lock" }}
      - run: export PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=1
      - run: sudo npm install -g pa11y
      - run: bundle install
      - run: bundle exec jekyll build
      - run: pa11y ./_site/
  # percy:
  #   docker:
  #     - image: circleci/ruby:2.6.3-node-browsers
  #   steps:
  #     - checkout
  #     - restore_cache:
  #         keys:
  #           - v1-dependencies-{{ checksum "Gemfile.lock" }}
  #     - run:
  #         name: Install dependencies
  #         command: >
  #           bundle check --path vendor/bundle || bundle install --deployment
  #     - run:
  #         name: Build the static site
  #         command: bundle exec jekyll build
  #     - run:
  #         name: Visual Tests
  #         command: npx percy snapshot _site
  build:
    docker:
      - image: circleci/ruby:2.6.3-node
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "Gemfile.lock" }}
      - run:
          name: Install dependencies
          command: >
            bundle check --path vendor/bundle || bundle install --deployment
      - run: bundle exec jekyll build
  deploy:
    docker:
      - image: circleci/ruby:2.6.3-node
    environment:
      DIST_FOLDER: '_site'
      PUBLISH_BRANCH: 'gh-pages'
      REMOTE: origin
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "Gemfile.lock" }}
      - add_ssh_keys:
          fingerprints:
            - "0a:ae:09:0f:16:52:4d:70:20:38:5b:2b:7c:ec:94:82"

      - run: git config --global user.email "labplus@dia.govt.nz"
      - run: git config --global user.name "Lab Plus"
      - run:
          name: Install dependencies
          command: >
            bundle check --path vendor/bundle || bundle install --deployment
      - run:
        name: Building static site
        command: bundle exec jekyll build

      # - run: ./bin/deploy-to-gh-pages $CIRCLE_BUILD_NUM
      - run:
          name: setting release number
          command: export RELEASE="release-${CIRCLE_BUILD_NUM}"
      - run:
          name: setting git user for deployment
          command: GIT_USER=$(git config user.name)
      - run:
        name: Creating branch
        command: git checkout -b "${RELEASE}" > /dev/null
      - run:
        name: Committing _site into branch ${RELEASE}
        comand: git add --force _site && git commit "${DIST_FOLDER}" -m "Build $CIRCLE_BUILD_NUM of ${DIST_FOLDER} by ${GIT_USER}" > /dev/null


workflows:
  version: 2
  # visual:
  #   jobs:
  build_and_deploy:
    jobs:
      - yamllint
      - build
      - accessibility:
          requires: build
      # - percy:
      #     requires: build
      - test:
          requires: build
      - deploy:
          requires:
            - build
            - test
          filters:
            branches:
              only: circleci-deploy

#!/bin/bash

bundle exec jekyll build

set -euv

FILES=$(find "./_site" | grep html)

for file in $FILES
do
  echo "============== checking $file"
  ./node_modules/.bin/pa11y-ci "$file" || exit 1
done